import { ITreeNode } from "./typing";

export namespace Tree {
  function printTree(tree: ITreeNode) {
    
  }
  function getDepth(tree: ITreeNode) {
    let depth = 0;
    let current: ITreeNode | void = undefined;
    while (current) {
      
    }
  }
}